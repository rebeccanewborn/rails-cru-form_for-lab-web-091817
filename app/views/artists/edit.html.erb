<h2>Edit Artist Form</h2>

<%= form_for(@artist) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, value: @artist.name %>
  <%= f.label :bio %>
  <%= f.text_field :bio, value: @artist.bio %>
  <%= f.submit %>
<% end %>
